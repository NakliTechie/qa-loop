node_modules/

# secrets — never commit (but keep the example template)
.env
.env.*
!.env.example

# scratch run output (curated copies are published under stress/)
runs/

# scratch artifacts / logs at repo root
/out.html
/round-*.png
/round-*.html
/report-*.json
/*.log
